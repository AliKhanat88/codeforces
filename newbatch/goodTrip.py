[import sys
input = sys.stdin.readline

mod = 10 ** 9+7
def solve():
    n, m, k = map(int, input().split())
    d = (n * (n + 1)) // 2
    mult_d = pow(d, mod-2, mod)
    ]
    ans = 0
    for i in range(k):


for t in range(int(input())):
    solve()